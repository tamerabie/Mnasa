# البنية التقنية للنظام (System Architecture)
## المخرج 10 من 20 — ELH-PRIMARY1 (الترم الأول)

**مبني على:** تصميم قاعدة البيانات (المخرج 9)، القيود التقنية والأدوات المجانية (SRS قسم 8.3)، وقيد بيئة العمل من الموبايل فقط (NFR-19).

---

## 1. المخطط العام للبنية

```
┌─────────────────────────────────────────────────────────────┐
│                     المستخدم (متصفح / PWA)                    │
│        عالم الطالب (Static SPA)  │  لوحة تحكم المدير (SPA)     │
└───────────────┬─────────────────────────────┬─────────────────┘
                │ HTTPS                        │ HTTPS
                ▼                              ▼
        ┌───────────────────────────────────────────┐
        │      GitHub Pages (استضافة ثابتة فقط)        │
        │   يخدم ملفات Frontend المبنية مسبقًا (Build)   │
        └───────────────┬───────────────────────────┘
                         │ طلبات API (فقط عند الحاجة لبيانات/مصادقة)
                         ▼
        ┌───────────────────────────────────────────┐
        │                 Supabase                    │
        │  ┌─────────┐ ┌──────────┐ ┌───────────────┐ │
        │  │  Auth   │ │ Postgres │ │    Storage     │ │
        │  │(طالب/مدير)│ │  + RLS   │ │(تسجيلات صوتية، │ │
        │  │         │ │          │ │  PDF تقارير)   │ │
        │  └─────────┘ └──────────┘ └───────────────┘ │
        │  ┌───────────────────────────────────────┐  │
        │  │   Edge Functions (منطق خادم حساس)       │  │
        │  │  - تحليل النطق                          │  │
        │  │  - تحليلات Joe / AI Analytics           │  │
        │  │  - توليد تقارير PDF                     │  │
        │  │  - إرسال البريد الإلكتروني للتقارير      │  │
        │  └─────────────────┬─────────────────────┘  │
        └────────────────────┼─────────────────────────┘
                             │ HTTPS (مفاتيح API مخزّنة كـ Secrets)
                             ▼
              ┌──────────────────────────────┐
              │   خدمات AI خارجية (طرف ثالث)   │
              │  - تحليل/تصحيح النطق (Speech)  │
              │  - نموذج لغوي لتوصيات Joe       │
              └──────────────────────────────┘
```

**المبدأ الأهم:** GitHub Pages يخدم **ملفات ثابتة فقط** (HTML/JS/CSS مبنية مسبقًا). كل منطق حساس (قراءة/كتابة بيانات، تحليل، إرسال بريد) يمر حصرًا عبر Supabase — لا تُستدعى أي خدمة AI خارجية مباشرة من المتصفح، لأن ذلك يعني كشف مفاتيح API (يخالف NFR-10).

---

## 2. طبقة Frontend

| العنصر | القرار |
|---|---|
| نوع التطبيق | SPA (Single Page Application) مبني مسبقًا (Static Build) — يعمل بدون خادم Node دائم |
| تطبيقان منفصلان أم واحد بـ Routing؟ | تطبيق واحد بـ Routing يفصل بوضوح بين `/` (عالم الطالب) و`/admin` (لوحة المدير) — كل منهما يحمّل حزمة موارد (Bundle) مختلفة لتقليل حجم التحميل (Code Splitting)، يحقق NFR-11/NFR-12 |
| PWA | Service Worker لتخزين مؤقت لآخر وحدة تم الدخول لها + الواجهة الأساسية (Shell) — يحقق NFR-14 |
| إدارة الحالة | حالة محلية بسيطة للتقدم أثناء الجلسة + مزامنة فورية مع Supabase عند كل إنجاز مرحلة (لا تُفقد بيانات الطفل عند إغلاق المتصفح فجأة) |

---

## 3. طبقة Backend (Supabase)

### 3.1 Auth
- حساب الطالب: دخول مبسّط (اسم مستخدم + صورة/PIN بدل كلمة مرور معقدة، مناسب لعمر 6 سنوات) — يُنشأ الحساب فعليًا من طرف المدير فقط (لا تسجيل ذاتي مفتوح).
- حساب المدير: بريد إلكتروني + كلمة مرور قياسية (Supabase Auth).
- **لا يوجد نوع حساب "guardian" في نظام Auth إطلاقًا** — يتوافق مع قرار عدم وجود لوحة تحكم لولي الأمر.

### 3.2 Postgres + RLS
كما هو مفصّل بالكامل في Database Design (المخرج 9) — كل الجداول محمية بسياسات RLS صارمة.

### 3.3 Storage
- Bucket منفصل للتسجيلات الصوتية (خاص، غير عام) — روابط مؤقتة موقّعة (Signed URLs) فقط عند الحاجة الفعلية للتحليل.
- Bucket منفصل لتقارير PDF — قابل للوصول فقط من Edge Function الإرسال ومن لوحة المدير.

### 3.4 Edge Functions (منطق الخادم الحساس)
| الدالة | الوظيفة | من يستدعيها |
|---|---|---|
| `analyze-pronunciation` | استقبال التسجيل الصوتي → استدعاء خدمة تحليل النطق → إرجاع نتيجة + تخزينها | تطبيق الطالب (بعد كل محاولة Phonics) |
| `joe-analysis` | تحليل أداء الطالب الشامل وتوليد توصية شخصية (FR-1.4–1.6) | مجدول دوريًا + عند إنهاء كل وحدة |
| `generate-report-pdf` | تجميع بيانات طالب معيّن وتحويلها لملف PDF | لوحة المدير (عند الطلب) |
| `send-report-email` | إرسال ملف PDF المُولَّد لبريد ولي الأمر | لوحة المدير فقط، بعد قرار المدير الصريح (FR-6.3) |
| `weekly-monthly-rollup` | تجميع تحليلات دورية (FR-7.5/7.6) | مجدول تلقائيًا (Cron) |

كل دالة تتحقق من هوية المستخدم المستدعي (JWT) وصلاحيته قبل التنفيذ — لا دالة تثق بأي بيانات قادمة من العميل بدون تحقق من صاحبها.

---

## 4. التكامل مع خدمات AI الخارجية

- الاتصال بخدمات تحليل النطق و/أو النموذج اللغوي (لتوليد توصيات Joe) يتم **فقط** من داخل Edge Functions، حيث تُخزَّن مفاتيح API كمتغيرات بيئة سرّية (Environment Secrets) في Supabase — لا تظهر أبدًا في كود الـ Frontend المنشور على GitHub Pages.
- التفاصيل الدقيقة لاختيار الخدمة (نطق) ونموذج اللغة (توصيات) تُحسم في AI Architecture (المخرج 11) وPhonics Architecture (المخرج 12).

---

## 5. توافق مع بيئة العمل من الموبايل (NFR-19)

- **بناء ونشر Frontend:** عبر GitHub Actions (يعمل تلقائيًا عند أي تعديل من محرر GitHub على الويب من الموبايل — لا حاجة لتشغيل أوامر Build يدويًا من جهاز Tamer).
- **تعديل المحتوى النصي/الإعدادات البسيطة:** ملفات تهيئة (JSON/Markdown) قابلة للتعديل مباشرة من محرر GitHub على المتصفح، بدون الحاجة لفتح بيئة تطوير كاملة.
- **إدارة Supabase:** لوحة تحكم Supabase نفسها متصفح-محور بالكامل، تعمل من الموبايل بدون Terminal.
- لا خطوة واحدة في هذه البنية تتطلب سطر أوامر (CLI) إجباريًا للتشغيل اليومي — الـ CLI (إن استُخدم) يكون فقط للإعداد الأولي لمرة واحدة، ويمكن لـ Claude تنفيذه نيابة عن Tamer عند الحاجة.

---

## 6. ملخص القرارات التقنية

| القرار | السبب |
|---|---|
| Static Frontend على GitHub Pages | مجاني، يتوافق مع الموقع الرئيسي الحالي، لا يحتاج خادم دائم |
| Supabase كخلفية كاملة | مجاني ضمن حدود معقولة، يوفر Auth + DB + Storage + Functions في مكان واحد |
| كل منطق حساس في Edge Functions | يمنع كشف مفاتيح API ويطبق التحقق من الصلاحيات مركزيًا |
| لا حساب Auth لولي الأمر | يطابق القرار المعتمد سابقًا (إدارة فقط للمدير) |
| PWA بتخزين مؤقت محدود | يوازن بين العمل دون اتصال (NFR-14) وبساطة الصيانة |

---

## الخطوة التالية

**الخطوة 11 — AI Architecture**: تحديد الخدمة/النموذج الفعلي لتحليلات Joe والتوصيات الشخصية، وكيفية تغذيته ببيانات الطالب من قاعدة البيانات أعلاه.

هذا المستند يحتاج مراجعتك واعتمادك قبل بدء الخطوة 11.
