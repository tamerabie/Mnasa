# دليل تنفيذ المرحلة 0 — الأساس التقني
## من الموبايل بالكامل، بدون Terminal

**الهدف النهائي لهذه المرحلة:** صفحة "Hello World" منشورة فعليًا وتتصل بقاعدة بيانات حقيقية بنجاح.

---

## الخطوة 1 — إنشاء مستودع GitHub منفصل

1. من متصفح الموبايل، ادخل github.com وسجّل دخولك.
2. اضغط "+" أعلى الصفحة → **New repository**.
3. اسم مقترح: `Mnasa` (مستودع **منفصل تمامًا** عن موقعك `Really` الحالي).
4. اجعله **Public** (مطلوب لـ GitHub Pages المجاني).
5. فعّل "Add a README file".
6. اضغط **Create repository**.

---

## الخطوة 2 — تفعيل GitHub Pages

1. داخل المستودع الجديد → **Settings** → **Pages** (بالقائمة الجانبية).
2. تحت "Build and deployment" → Source: اختر **Deploy from a branch** (وليس GitHub Actions — دي بتحتاج ملف Workflow مخصص مش هنحتاجه إلا لما نضيف خطوة بناء (Build) فعلية بمراحل لاحقة).
3. Branch: اختر **main** والمجلد **/ (root)**.
4. **Save**.
5. انتظر دقيقة أو دقيقتين، وهيظهر لك رابط الموقع أعلى نفس الصفحة.

---

## الخطوة 3 — إنشاء مشروع Supabase

1. ادخل supabase.com من المتصفح وسجّل دخولك (يمكن بحساب GitHub نفسه).
2. **New Project** → اختر اسمًا مثل `Mnasa`.
3. اختر كلمة مرور قوية لقاعدة البيانات واحفظها بمكان آمن (ليست كلمة مرور حساب Supabase نفسه، بل لقاعدة البيانات).
4. انتظر حتى يكتمل إنشاء المشروع (دقيقة أو دقيقتين).

---

## الخطوة 4 — تنفيذ سكريبت قاعدة البيانات

1. من لوحة Supabase → **SQL Editor** (بالقائمة الجانبية).
2. **New query**.
3. افتح ملف `schema.sql` المرفق، انسخ **كل محتواه**، والصقه في المحرر.
4. اضغط **Run**.
5. تأكد من ظهور رسالة نجاح بدون أخطاء. إن ظهر أي خطأ، انسخه وابعته لي هنا وهساعدك تحله فورًا.
6. تحقق: **Table Editor** بالقائمة الجانبية → يجب أن تشاهد كل الجداول (units، students، admins... إلخ) وجدول `units` يحتوي بالفعل 6 صفوف (المحطات الست).

---

## الخطوة 5 — إنشاء أول حساب مدير

1. من لوحة Supabase → **Authentication** → **Users** → **Add user**.
2. أدخل بريدك الإلكتروني وكلمة مرور قوية → **Create user**.
3. انسخ الـ **User UID** الذي يظهر بجانب المستخدم الجديد.
4. ارجع لـ **SQL Editor** → **New query** والصق (مع استبدال UID والبريد بما نسخته):
   ```sql
   insert into admins (id, email, role)
   values ('ضع-الـ-UID-هنا', 'بريدك@example.com', 'admin');
   ```
5. **Run**.

---

## الخطوة 6 — الحصول على مفاتيح الاتصال

1. من لوحة Supabase → **Project Settings** → **API**.
2. انسخ قيمتين وسجّلهما بمكان آمن مؤقتًا:
   - **Project URL**
   - **anon public key**

---

## الخطوة 7 — ملاحظة مهمة عن نوعي المفاتيح

Supabase يعطيك نوعين من المفاتيح، ومهم تفرّق بينهم:
- **anon public key:** آمن الظهور بكود الـ Frontend مباشرة (Supabase مصمَّم على أساس إن الحماية الفعلية من RLS، مش من إخفاء هذا المفتاح).
- **service_role key:** خطير جدًا، له صلاحية تجاوز RLS بالكامل — **هذا فقط** يُخزَّن كـ Secret بـ GitHub Actions ولا يظهر إطلاقًا بأي كود منشور. لن نحتاجه في هذه المرحلة المبكرة.

لذلك: في صفحة الاختبار البسيطة دي، هنحط الـ Project URL والـ anon public key مباشرة بالكود — ده طبيعي وآمن.

---

## الخطوة 8 — رفع صفحة الاختبار الأولى

ملف `index.html` المرفق بيتحقق من الاتصال الفعلي بقاعدة البيانات (يعرض "الاتصال ناجح ✅" ويظهر أسماء المحطات الست إن نجح).

**من محرر GitHub على الويب:**
1. داخل المستودع → **Add file** → **Create new file**.
2. اسم الملف: `index.html`.
3. الصق المحتوى المرفق.
4. بأعلى الملف هتلاقي سطرين:
   ```js
   const SUPABASE_URL = "ضع-الـ-Project-URL-هنا";
   const SUPABASE_ANON_KEY = "ضع-الـ-anon-public-key-هنا";
   ```
   استبدلهم بالقيمتين اللي نسختهم بالخطوة 6.
5. **Commit changes** مباشرة على الفرع الرئيسي.

بعد دقيقة أو دقيقتين، الموقع هيبقى شغال على:
`https://tamerabie.github.io/Mnasa/`

---

## معيار نجاح المرحلة 0 بالكامل

- [ ] المستودع منشور فعليًا على GitHub Pages.
- [ ] صفحة الاختبار تعرض "الاتصال ناجح ✅" وأسماء المحطات الست الظاهرة بالعربي.
- [ ] حساب المدير الأول موجود وتم التحقق من دخوله (اختبار لاحق بالمرحلة 1).

لما توصل هنا، ابعتلي "خلصت المرحلة 0" ونبدأ المرحلة 1 (الهيكل العام والهوية البصرية) فورًا.
