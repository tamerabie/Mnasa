# التصميم الأمني (Security Design)
## المخرج 17 من 20 — ELH-PRIMARY1 (الترم الأول)

**الهدف:** توثيق أمني نهائي شامل، يجمع ويُفصّل كل ما أُشير له كـ"يُحسم لاحقًا" في المخرجات 1، 9، 10، 11، 12 — هذا المستند هو المرجع الأمني الوحيد النهائي لكل قرارات التنفيذ.

---

## 1. سياسات الاحتفاظ بالبيانات (Data Retention) — القرارات النهائية

| نوع البيانات | مدة الاحتفاظ | بعدها |
|---|---|---|
| تسجيلات صوتية خام (Phonics) | 30 يومًا من تاريخ التسجيل | حذف تلقائي دائم (Cron يومي)؛ يبقى فقط تصنيف النجوم بـ`phonics_attempts` |
| أحداث سلوكية خام (`behavior_events`) | 90 يومًا | تُجمَّع كمؤشرات إحصائية بـ`ai_analytics` ثم تُحذف السجلات الخام التفصيلية |
| تقارير PDF مُرسَلة | حتى نهاية العام الدراسي + 6 أشهر | أرشفة أو حذف حسب طلب المدير |
| حساب طالب مُغادِر نهائيًا | يُطلب من المدير تأكيد صريح للحذف | حذف كامل (Hard Delete) لكل الجداول المرتبطة خلال 30 يومًا من التأكيد — لا بيانات "زومبي" متبقية |
| سجلات محادثة Joe (نصوص الأسئلة الحرة) | 14 يومًا (لأغراض تحسين الجودة فقط) | حذف تلقائي؛ لا تُستخدم بعد ذلك حتى للتدريب |

**مبدأ عام:** لا بيانات طفل تُحفَظ "للأبد افتراضيًا" — كل جدول له مدة صلاحية محددة صراحة أعلاه، وليس تركها مفتوحة.

---

## 2. المصادقة (Authentication) — التفاصيل النهائية

### حساب المدير
- بريد إلكتروني + كلمة مرور (Supabase Auth، تشفير Bcrypt/Argon2 داخليًا حسب معيار Supabase).
- **يُنصَح بتفعيل** مصادقة ثنائية (2FA) لاحقًا كتحسين أمني، خصوصًا لأن هذا الحساب له وصول كامل لبيانات أطفال.
- انتهاء صلاحية الجلسة (Session Timeout): 30 دقيقة من عدم النشاط بلوحة التحكم (بيانات حساسة، لا تُترك جلسة مفتوحة طويلًا على جهاز مشترك).

### حساب الطالب
- دخول مبسّط (اسم مستخدم يُنشئه المدير + صورة/PIN من 4 عناصر بصرية بدل كلمة مرور نصية).
- الجلسة تبقى مفتوحة لفترة أطول (يوم دراسي كامل تقريبًا) لتقليل الاحتكاك مع طفل صغير، لكن **بدون** أي صلاحية وصول لبيانات حساسة أصلًا (الطالب لا يرى إلا بياناته المبسّطة).
- **لا يوجد "نسيت كلمة المرور" ذاتي للطالب** — أي مشكلة دخول تُحل عبر المدير فقط (يمنع هندسة اجتماعية موجهة لطفل).

---

## 3. JWT وإدارة الجلسات

- JWT قصير العمر (Access Token: ساعة واحدة) + Refresh Token آمن (HttpOnly Cookie، ليس LocalStorage — يمنع سرقته عبر XSS).
- كل Edge Function تتحقق من توقيع JWT وصلاحية الدور (Role Claim) قبل أي عملية، بما يتوافق مع RLS بقاعدة البيانات كطبقة حماية مزدوجة (Defense in Depth).

---

## 4. الحماية من XSS وSQL Injection — التنفيذ الفعلي

- **SQL Injection:** يُستخدم Supabase Client (Parameterized Queries تلقائيًا) — لا استعلامات SQL خام مبنية بدمج نصوص (String Concatenation) في أي مكان بالكود.
- **XSS:** 
  - كل محتوى نصي من مصدر مستخدم (سؤال الطالب لـJoe، بيانات نموذج المدير) يُعقَّم (Sanitize) قبل أي عرض بالواجهة.
  - Content Security Policy (CSP) صارمة على مستوى الصفحة تمنع تحميل سكريبتات من مصادر غير موثوقة.
  - لا استخدام لـ `dangerouslySetInnerHTML` أو مكافئه إلا بعد تعقيم صريح موثّق بالكود.

---

## 5. أمان الـ API (يُفصّل NFR-7)

- **Rate Limiting:** لكل Edge Function حد أقصى طلبات/دقيقة لكل مستخدم (مثال: `joe-chat` حد 10 طلبات/دقيقة لكل طالب لمنع إساءة الاستخدام أو استنزاف تكلفة AI).
- **CORS:** مقيّد فقط لدومين المنصة الرسمي (GitHub Pages) — لا يُسمح باستدعاء الـ API من أي دومين آخر.
- **التحقق من المُدخلات (Input Validation):** كل مدخل من العميل يُتحقق من نوعه وحجمه قبل المعالجة (مثال: حجم أقصى للملف الصوتي المرفوع، طول أقصى لنص سؤال Joe).

---

## 6. التحكم بالصلاحيات (RBAC) — ملخص نهائي موحّد

| الدور | ماذا يملك |
|---|---|
| Student | قراءة/كتابة بياناته الخاصة فقط، عبر واجهات محدودة مسبقًا (لا استعلام حر) |
| Admin | صلاحية كاملة على كل الجداول والوظائف الإدارية — **الدور الإداري الوحيد بالنظام بالكامل** (تأكيدًا للمبدأ المعتمد) |
| (لا يوجد) Guardian | لا حساب، لا صلاحية، لا وصول من أي نوع — تواصل بالبريد فقط |

---

## 7. حماية بيانات الطفل (تفصيل NFR-9)

- لا بيانات تعريف كاملة (بدون اسم عائلة، بدون تاريخ ميلاد دقيق إن أمكن الاستغناء عنه) — الحد الأدنى الضروري فقط (Data Minimization).
- لا صور حقيقية للطفل تُرفع أو تُخزَّن في أي مرحلة (الأفاتار Adam افتراضي دائمًا، وليس صورة حقيقية).
- عدم إرسال أي بيانات تعريف شخصية لخدمات AI الخارجية (تم تأكيده بالمخرج 11) — يُعاد التأكيد هنا كسياسة أمنية ملزمة، وليس اقتراحًا تقنيًا فقط.
- بريد ولي الأمر (`guardians.contact_email`) يُخزَّن مشفّرًا في قاعدة البيانات (Encryption at Rest إضافية فوق تشفير Supabase الافتراضي).

---

## 8. خطة الاستجابة لاختراق محتمل (Incident Response — الأساسيات)

1. عزل فوري: تعطيل مفاتيح API المتأثرة فورًا عبر Supabase Secrets.
2. تدوير (Rotate) كل الأسرار والمفاتيح المرتبطة.
3. مراجعة سجلات الوصول (Audit Logs) لتحديد نطاق التأثير الفعلي.
4. إشعار المدير فورًا بالحادثة وبنطاقها.
5. توثيق الحادثة وإجراء تصحيحي دائم يمنع تكرارها.

**ملاحظة:** إشعار أولياء الأمور عند أي حادثة تمس بياناتهم فعليًا هو قرار قانوني/أخلاقي يحتاج مراجعة عند الحاجة الفعلية، وليس إجراء تقنيًا بحتًا يُحسم مسبقًا هنا.

---

## 9. اختبارات أمنية أساسية مطلوبة قبل الإطلاق (تمهيد لـ Testing Strategy)

- اختبار RLS: محاولة وصول طالب A لبيانات طالب B مباشرة (يجب أن تُرفض دائمًا).
- اختبار محاولة تجاوز صلاحية عبر تعديل JWT يدويًا.
- اختبار حقن نصي (Prompt Injection) على Joe للتأكد من ثبات System Prompt.
- اختبار تحميل ملفات ضارة (غير صوتية) على مسار رفع Phonics.
- فحص تسريب مفاتيح API في أي كود Frontend منشور فعليًا على GitHub Pages (Static Scan دوري).

---

## الخطوة التالية

**الخطوة 18 — Testing Strategy**: خطة اختبار شاملة (وظيفية، أمنية كما أعلاه، أداء، تجربة مستخدم مع أطفال حقيقيين إن أمكن) قبل أي نشر فعلي.

هذا المستند يحتاج مراجعتك واعتمادك قبل بدء الخطوة 18.
