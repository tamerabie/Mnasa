# تحليلات السلوك (Behavioral Analytics)
## المخرج 14 من 20 — ELH-PRIMARY1 (الترم الأول)

**مبني على:** Learning Analytics (المخرج 13)، متطلبات Joe السلوكية (FR-1.5/FR-1.6)، Behavior Analytics بلوحة المدير (FR-7.4).
**الفرق عن المخرج 13:** هناك "ماذا يتعلم الطالب"، وهنا "كيف يتصرف وهو يتعلم" — الحماس، التردد، التركيز، إشارات الحاجة للدعم. لا علاقة مباشرة بالدرجات أو الدقة الأكاديمية.

---

## 1. إضافة مطلوبة على قاعدة البيانات (ملحق لـ Database Design)

يحتاج هذا المخرج جدول أحداث خفيف غير موجود صراحة بالمخرج 9:

### `behavior_events`
| الحقل | النوع | ملاحظات |
|---|---|---|
| id | UUID (PK) | |
| student_id | FK → students | |
| unit_id / stage | FK / enum (nullable) | أين حدث التفاعل |
| event_type | enum | انظر جدول القسم 2 |
| duration_ms | int (nullable) | لأحداث لها مدة (تردد، تركيز) |
| created_at | timestamp | |

**صلاحيات RLS:** المدير فقط يقرأ البيانات المجمّعة (Aggregated)؛ لا وصول مباشر للطالب لهذا الجدول؛ لا وصول خام غير مجمّع لأي طرف خارج تحليلات Joe والمدير (بيانات حساسة سلوكيًا).

---

## 2. أنواع الأحداث المُسجَّلة (Event Types)

| الحدث | ماذا يعني | الإشارة السلوكية |
|---|---|---|
| `stage_start_delay` | زمن التردد قبل بدء نشاط جديد | تردد محتمل / حاجة لتشجيع |
| `retry_triggered` | ضغط "حاول تاني" (Phonics أو لعبة) | ليس سلبيًا بذاته — إصرار وتحمّل |
| `voluntary_replay` | إعادة تشغيل أغنية/قصة طواعية دون إلزام | حماس وانخراط إيجابي قوي |
| `session_pause` | توقف الطالب منتصف مرحلة لأكثر من دقيقتين | احتمال فقدان تركيز أو مقاطعة خارجية |
| `stage_completion_time` | الزمن الفعلي المستغرق بمرحلة معينة | مؤشر تركيز (طويل جدًا أو قصير جدًا كلاهما يُلاحَظ) |
| `joe_help_requested` | الطالب استدعى Joe يدويًا | إشارة إيجابية على الاستقلالية (يطلب المساعدة بدل الجمود) |
| `abandonment_point` | خروج الطالب من التطبيق منتصف وحدة | نقطة يجب مراجعتها إن تكررت لدى عدة طلاب بنفس المكان |

---

## 3. المؤشرات السلوكية المُشتقة (للمدير ولـ Joe)

| المؤشر | الحساب | الاستخدام |
|---|---|---|
| مستوى الانخراط (Engagement) | نسبة `voluntary_replay` + `joe_help_requested` لكل جلسة | يغذّي "الفضول" في قسم علم نفس الطفل (SRS قسم 7) |
| مؤشر التردد (Hesitation Index) | متوسط `stage_start_delay` عبر آخر عدة جلسات | يُطلق تدخل Joe اللطيف اللحظي (قسم 4) |
| مدة التركيز الفعّالة | متوسط `stage_completion_time` بدون `session_pause` طويلة | يساعد المدير على فهم أنسب توقيت/طول جلسة للطالب |
| نقاط التوقف المتكررة (Common Drop-off Points) | تجميع `abandonment_point` عبر كل الطلاب لنفس الوحدة/المرحلة | إشارة تصميمية: هل مرحلة معينة تحتاج مراجعة تصميم (وليس الطالب هو "المشكلة") |

---

## 4. الاستخدام اللحظي (Real-Time) من Joe

على عكس Learning Analytics (تُحسب دفعة واحدة)، بعض إشارات السلوك تحتاج استجابة **لحظية**:

```
إن (stage_start_delay > حد معيّن، مثلاً 20 ثانية بلا أي تفاعل)
        │
        ▼
Joe يظهر تلقائيًا بلطف (وليس نافذة مقاطعة مفاجئة)
        │
        ▼
رسالة تشجيع بسيطة: "محتاج مساعدة يا بطل؟ 😊" + خيار لعرض تلميح
```

هذا التدفق **لا يمر عبر محرك التحليلات الثقيل** (المخرج 11) — بل منطق بسيط ومباشر بالـ Frontend/Edge Function خفيف لضمان الاستجابة الفورية.

---

## 5. مبدأ أخلاقي ملزم: لا تصنيف سلبي للطفل

- **ممنوع تمامًا** أي مصطلح أو تصنيف من نوع "طالب مشتّت" أو "طالب متردد" يُخزَّن أو يُعرض بصيغة تصف الطفل كصفة ثابتة — كل ما يُقاس هو **أنماط تفاعل مع محتوى محدد في وقت محدد**، قابلة للتغير دائمًا.
- عرض المدير لهذه البيانات يكون بصيغة "أنماط ملاحظة" وليس "تشخيص" — مثال: "لوحظ تردد متكرر عند بداية Phonics في آخر أسبوعين" — وليس "الطالب يعاني من ضعف تركيز".
- نقاط التوقف المتكررة (Drop-off) تُفسَّر أولًا كإشارة لمراجعة **تصميم المرحلة نفسها**، قبل افتراض أن المشكلة في الطفل.

---

## 6. عرض لوحة المدير (Behavior Analytics — FR-7.4)

| العنصر | الشكل |
|---|---|
| نظرة عامة لكل طالب | مؤشرات الانخراط والتردد والتركيز كرسم بياني بسيط عبر الوقت |
| تنبيه استباقي (اختياري) | إشعار للمدير فقط عند نمط متكرر واضح (وليس عند حدث واحد) — لتفادي إنذارات كاذبة متكررة |
| مقارنة عبر الوحدات | أي وحدة/مرحلة لديها أعلى معدل تردد أو توقف عبر كل الطلاب — إشارة لمراجعة تصميمية جماعية |

---

## الخطوة التالية

بحسب ترتيب المخرجات، القسم الخاص بـ "لوحة تحكم ولي الأمر" (المخرج 15 الأصلي) أصبح **Parent Reporting** فقط — تم تغطية محتواه بالفعل ضمن SRS (قسم 4.6) وDatabase Design (جدولا `guardians`/`reports`) وSystem Architecture (Edge Functions الإرسال)، بلا لوحة تحكم منفصلة تحتاج تصميمًا إضافيًا.

**الخطوة التالية الفعلية: 16 — Admin Dashboard** (تصميم تفصيلي نهائي لكل شاشات لوحة المدير، مبنيًا على كل التحليلات في المخرجين 13 و14).

هذا المستند يحتاج مراجعتك واعتمادك قبل بدء الخطوة 16.
