# البنية الذكية (AI Architecture)
## المخرج 11 من 20 — ELH-PRIMARY1 (الترم الأول)

**مبني على:** System Architecture (المخرج 10، طبقة Edge Functions)، متطلبات Joe (FR-1.1–1.9) وتحليلات AI (FR-8.1–8.9) من SRS.
**نطاق هذا المستند:** "عقل" Joe المحادثاتي والتحليلي بشكل عام. تحليل النطق الصوتي التفصيلي (Phonics) له معمارية خاصة منفصلة في المخرج 12 لأنه نظام مختلف تقنيًا (Speech-to-Text/Pronunciation Scoring).

---

## 1. نظامان ذكيان منفصلان داخل "Joe"

| النظام | الوظيفة | متطلبات الأداء |
|---|---|---|
| **Joe المحادثاتي (Conversational)** | شرح، إجابة أسئلة، إرشاد، تشجيع لحظي | استجابة سريعة (أقل من 2-3 ثوانٍ)، نبرة ثابتة حسب Character Bible |
| **محرك التحليلات (Analytics Engine)** | تحليل مستوى، سلوك، توصيات شخصية، تقارير دورية | يعمل في الخلفية/مجدولًا، لا يحتاج استجابة فورية |

كلاهما يستخدم نفس النموذج اللغوي الأساسي لكن بسياقات (Prompts) وتوقيتات تشغيل مختلفة تمامًا.

---

## 2. اختيار النموذج

- **النموذج اللغوي المقترح:** Claude (عائلة نماذج Anthropic)، عبر الـ API الرسمي، يُستدعى حصريًا من داخل Edge Functions (لا يظهر أي مفتاح API بالـ Frontend، حسب System Architecture).
- **سبب الاختيار:** دعم قوي للعربية والإنجليزية معًا (يحقق FR-1.2)، إمكانية ضبط شخصية ثابتة عبر System Prompt، ودعم مخرجات JSON منظّمة لمحرك التحليلات.
- **طبقة صوت منفصلة:** تحويل نص→صوت (TTS) وصوت→نص (STT) لأصوات الطفل، تُحسم تفاصيلها التقنية في Phonics Architecture (المخرج 12) لأنها مرتبطة مباشرة بتقييم النطق.

---

## 3. تصميم System Prompt الخاص بـ Joe

يُبنى System Prompt من عناصر ثابتة غير قابلة للتعديل من أي مدخل مستخدم:

```
أنت "Joe" — [شخصية ثابتة من Character Bible: حكيم، دافئ، صبور]
- تتحدث بالعربية أو الإنجليزية حسب لغة الطفل.
- جملك قصيرة جدًا جدًا (طفل عمره 6 سنوات).
- ممنوع نهائيًا: أي نبرة توبيخ، إحباط، أو تصحيح قاسٍ.
- كل خطأ يُقابَل بتشجيع + محاولة بديلة، أبدًا لا يُقال "غلط".
- ممنوع ذكر: "منهج"، "مصر"، "وزارة"، أو أي مصدر تعليمي خارجي.
- لا تخرج عن سياق عالم إل ومحتوى الوحدة الحالية.
- عند عدم اليقين، وجّه الطفل بلطف لمعلمه/معلمته بدلًا من الإجابة بثقة زائفة عن مواضيع خارج نطاقك.
```

**قاعدة أمان صارمة:** أي نص يكتبه الطالب (سؤال حر لـ Joe) يُعامَل كمُدخل غير موثوق (Untrusted Input) — لا يُسمح لمحتوى الطالب بتغيير التعليمات الأساسية لـ Joe (حماية من Prompt Injection)، ويُطبَّق فلتر محتوى قبل عرض أي رد على الطفل.

---

## 4. تدفق البيانات — Joe المحادثاتي

```
الطالب يسأل/يطلب شرحًا
        │
        ▼
Frontend يرسل: (سؤال الطالب + معرّف الوحدة/المرحلة الحالية فقط)
        │
        ▼
Edge Function `joe-chat`
        │  يبني السياق: System Prompt الثابت + بيانات الوحدة الحالية
        │  (بدون إرسال أي بيانات شخصية حساسة للنموذج)
        ▼
استدعاء Claude API
        │
        ▼
فلتر أمان على الرد (يتحقق من عدم وجود محتوى غير مناسب)
        │
        ▼
رد نصي قصير → تحويل TTS → عرض للطالب (نص + صوت)
```

---

## 5. تدفق البيانات — محرك التحليلات

```
مجدول (Cron) دوري + عند إنهاء كل وحدة
        │
        ▼
Edge Function `joe-analysis`
        │  يجمّع من قاعدة البيانات: unit_progress + phonics_attempts
        │  + rewards + الأداء التاريخي لنفس الطالب
        ▼
استدعاء Claude API — Prompt مختلف تمامًا:
   "حلل بيانات هذا الطالب وأرجع JSON يحتوي:
    نقاط قوة، نقاط ضعف، نشاط علاجي مقترح، رسالة توصية قصيرة"
        │
        ▼
تخزين النتيجة في `ai_analytics`
        │
        ├──→ تُعرض للطالب عبر Joe (مرحلة Personal Recommendation) — نسخة مبسّطة جدًا
        └──→ تُعرض للمدير عبر لوحة التحكم — نسخة تفصيلية كاملة (FR-8.1–8.9)
```

**ملاحظة تصميمية:** نفس التحليل يُقدَّم بمستويين مختلفين من التفصيل حسب من يراه (طفل مقابل مدير) — لا يُعرض للطالب أي مصطلح تحليلي مباشر ("دقة النطق 62%")، بل رسالة مشجعة مبسّطة فقط.

---

## 6. الرقابة والتكلفة (Cost & Rate Control)

- تخزين مؤقت (Caching) لشروحات الوحدات المتكررة (Learning Goal الثابت لكل وحدة لا يُعاد توليده بالذكاء الاصطناعي في كل مرة — يُكتب مسبقًا في Story World Design/المحتوى الفعلي).
- استخدام النموذج اللغوي التوليدي محصور في: المحادثة الحرة مع Joe + التحليلات الدورية + توليد التوصيات الشخصية — وليس توليد محتوى الوحدات الثابت (القصص، الألعاب) الذي يُصمَّم ويُراجَع مسبقًا من الفريق (يحافظ على جودة والتزام Story World Design).
- حد أقصى لعدد الأسئلة الحرة للطالب في الجلسة الواحدة، بحد يُعاد تلقائيًا (Rate Limiting على مستوى Edge Function)، يحقق NFR-7.

---

## 7. معايير الأمان الخاصة بالأطفال

- عدم إرسال أي بيانات تعريف شخصية حساسة (اسم كامل، بريد ولي الأمر) ضمن الطلبات لنموذج AI الخارجي — فقط معرّف داخلي مجهول ومحتوى تعليمي.
- سجل كل تفاعل مع Joe يُخزَّن مؤقتًا لأغراض التحليل فقط، بسياسة احتفاظ محدودة (تُحسم في Security Design، المخرج 17).
- أي رد من AI يحتوي احتمال محتوى غير لائق (حتى لو نادر الحدوث) يُوقَف عبر فلتر المحتوى قبل الوصول للطفل نهائيًا — لا اعتماد كامل على حسن نية النموذج وحده.

---

## الخطوة التالية

**الخطوة 12 — Phonics Architecture**: تصميم النظام التقني الخاص بتحليل النطق تحديدًا (STT، تقييم الدقة الصوتية، آلية التصحيح الفوري) بشكل منفصل ومفصّل.

هذا المستند يحتاج مراجعتك واعتمادك قبل بدء الخطوة 12.
