# دليل تنفيذ المرحلة 1 — الهيكل العام والهوية البصرية

**معيار نجاح هذه المرحلة (من Roadmap):** شاشة تسجيل دخول (طالب/مدير) بالهوية البصرية الكاملة. دخول المدير هيبقى شغال فعليًا (متصل بحساب المدير من المرحلة 0). دخول الطالب شكله جاهز بالكامل، لكن المنطق الفعلي هيُبنى بمرحلة لاحقة.

---

## الملفات المرفقة (6 ملفات + مجلد صور)

1. `styles.css` — كل الهوية البصرية (الألوان، الخطوط، الأزرار) من UI Design System.
2. `index.html` — الصفحة الرئيسية (اختيار طالب/مدير).
3. `student-login.html` — شاشة دخول الطالب (بصرية فقط).
4. `admin-login.html` — شاشة دخول المدير (**شغالة فعليًا**، متصلة بـ Supabase).
5. `admin-dashboard-placeholder.html` — صفحة تأكيد نجاح الدخول (مؤقتة لحد ما نبني اللوحة كاملة).
6. مجلد `assets/` — يحتوي شعار المنصة وكل صور الشخصيات المعتمدة (Adam, Leo, Joe بحالتيه, Bella, Roro, Pinky).

---

## خطوات الرفع (من محرر GitHub على الويب)

### 1. رفع مجلد الصور أولًا
1. من صفحة المستودع الرئيسية (فرع `elh`) → **Add file** → **Upload files**.
2. اسحب/اختر كل الصور الموجودة بمجلد `assets` المرفق.
3. **مهم جدًا:** قبل الـ Commit، لازم تتأكدي إن مسار الرفع هو `assets/` — يعني لو GitHub مسمحش تعملي مجلد مباشرة من "Upload files"، اكتبي اسم كل ملف بالصيغة: `assets/logo.png`, `assets/adam.jpg`, `assets/leo.jpg`, `assets/joe-wave.png`, `assets/joe-think.png`, `assets/bella.png`, `assets/roro.png`, `assets/pinky.png` — GitHub هيعمل المجلد تلقائيًا.
4. **Commit changes**.

### 2. رفع باقي الملفات (بنفس الطريقة، في جذر المستودع مباشرة)
- ارفعي: `styles.css`, `index.html`, `student-login.html`, `admin-login.html`, `admin-dashboard-placeholder.html`.
- **ملاحظة:** `index.html` موجود بالفعل من المرحلة 0 (صفحة الاختبار) — هيسألك GitHub "تستبدلي الملف الموجود؟" → اختاري **نعم/Commit** عشان يحل محله بصفحة الدخول الجديدة.

### 3. تعديل مفاتيح Supabase داخل `admin-login.html`
1. افتحي الملف من داخل المستودع → أيقونة القلم (Edit).
2. دوّري على السطرين:
   ```js
   const SUPABASE_URL = "ضع-الـ-Project-URL-هنا";
   const SUPABASE_ANON_KEY = "ضع-الـ-anon-public-key-هنا";
   ```
3. استبدليهم بنفس القيمتين اللي استخدمتيهم في صفحة اختبار المرحلة 0.
4. **Commit changes**.

---

## الاختبار

1. افتحي: `https://rabieng.github.io/elh-primary1/`
2. المفروض تشوفي الصفحة الرئيسية بشعار عالم إل وزرّي "أنا طالب" / "أنا مدير".
3. جربي "أنا طالب" → شاشة دخول ودّية بصورة Joe.
4. جربي "أنا مدير" → دخلي بنفس البريد وكلمة المرور اللي عملتيهم بالمرحلة 0 (الخطوة 5) → المفروض توصلي لصفحة "✅ تم الدخول بنجاح".

---

## لو ظهرت مشكلة

ابعتيلي:
- رابط الصفحة اللي مش شغالة.
- سكرين شوت لو فيه رسالة خطأ.

وهحلها معاكِ فورًا زي ما عملنا بالمرحلة 0.

---

لما توصلي لـ"تم الدخول بنجاح" فعليًا، قولي "خلصت المرحلة 1" ونبدأ **المرحلة 2** — أول وحدة كاملة شغالة (بوابة إل) من أول محطة لآخر توصية شخصية 🎉
