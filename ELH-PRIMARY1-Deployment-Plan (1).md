# خطة النشر (Deployment Plan)
## المخرج 19 من 20 — ELH-PRIMARY1 (الترم الأول)

**مبني على:** System Architecture (المخرج 10)، Testing Strategy (المخرج 18)، قيد العمل من الموبايل (NFR-19).
**الهدف:** خطوات نشر فعلية وقابلة للتنفيذ حرفيًا من متصفح الموبايل، بدون Terminal.

---

## 1. البيئات (Environments)

| البيئة | الغرض | الرابط |
|---|---|---|
| **Development** | تطوير يومي، فروع Git منفصلة | معاينة مؤقتة تلقائية لكل Pull Request |
| **Staging** | اختبار كامل قبل النشر النهائي (Testing Strategy قسم 7 و8) | رابط GitHub Pages فرعي أو نطاق فرعي مؤقت |
| **Production** | الموقع الفعلي المنشور للطلاب والمدير | نطاق ELH-PRIMARY1 النهائي، منفصل عن الموقع التعليمي الرئيسي الحالي لـTamer |

**قرار مهم:** هذا المشروع يُنشر على مستودع GitHub منفصل تمامًا عن الموقع التعليمي الرئيسي (`tamerabie-official.github.io/Really/`) — لا دمج بينهما، لتفادي أي تعارض أو مخاطرة على الموقع القائم بالفعل.

---

## 2. خطوات الإعداد الأولي (لمرة واحدة فقط)

كل خطوة أدناه تُنفَّذ من متصفح الموبايل عبر واجهات الويب الرسمية (GitHub.com وSupabase.com) — لا حاجة لأي تطبيق أو أداة سطر أوامر:

1. إنشاء مستودع GitHub جديد مخصص لـELH-PRIMARY1.
2. تفعيل GitHub Pages من إعدادات المستودع (Settings → Pages).
3. إنشاء مشروع جديد على Supabase (لوحة تحكم ويب بالكامل).
4. تطبيق مخطط قاعدة البيانات (الجداول من المخرج 9) عبر محرر SQL بلوحة Supabase مباشرة من المتصفح.
5. تفعيل سياسات RLS لكل جدول (نسخ/لصق أوامر SQL جاهزة، بدون كتابة يدوية معقدة).
6. إضافة مفاتيح Supabase ومفاتيح خدمات AI كـ **Secrets** في إعدادات GitHub Actions (Settings → Secrets and variables) — لا تُكتب أبدًا داخل الكود مباشرة.
7. ربط نطاق مخصص (Custom Domain) إن وُجد، عبر إعدادات GitHub Pages.

---

## 3. خط أنابيب النشر المستمر (CI/CD)

```
Push/تعديل عبر محرر GitHub على الويب
        │
        ▼
GitHub Actions يبدأ تلقائيًا:
   1. تثبيت الاعتماديات (Dependencies)
   2. تشغيل اختبارات Smoke (Testing Strategy قسم 8)
   3. إن نجحت → بناء (Build) نسخة الإنتاج
   4. إن فشلت → إيقاف النشر تلقائيًا + إشعار
        │
        ▼
نشر تلقائي لـ GitHub Pages (Production)
```

كل هذا يحدث تلقائيًا خلف الكواليس بعد أي تعديل من محرر GitHub على الموبايل مباشرة — لا خطوة يدوية إضافية مطلوبة من Tamer عند كل تحديث محتوى بسيط.

---

## 4. قائمة تحقق ما قبل الإطلاق الرسمي (Launch Checklist)

- [ ] كل الاختبارات الآلية (Smoke Tests) خضراء.
- [ ] فحص الامتثال النصي (لا ذكر منهج/وزارة) خالٍ من أي نتيجة.
- [ ] مراجعة بصرية كاملة لكل شاشة (لا قوس قزح، لا ألوان كئيبة).
- [ ] جلسة اختبار مع أطفال حقيقيين تمت ونتائجها طُبِّقت.
- [ ] سياسات RLS مُفعَّلة ومُختبَرة على كل جدول بدون استثناء.
- [ ] لا مفاتيح API ظاهرة بأي ملف منشور (فحص Static Scan نهائي).
- [ ] حساب المدير الأول تم إنشاؤه واختباره فعليًا.
- [ ] Google Analytics مفعّل لتتبع الاستخدام العام (بدون بيانات تعريف شخصية للطفل).

---

## 5. خطة التراجع (Rollback Plan)

- GitHub Pages يحتفظ بسجل كل نشر سابق — التراجع لنسخة سابقة يتم بضغطة واحدة من GitHub Actions (Re-run/Revert deployment) بدون الحاجة لإعادة بناء شيء يدويًا.
- أي تغيير بقاعدة البيانات (Migration) يُكتب دائمًا بصيغة قابلة للتراجع (Reversible Migration) — لا تعديل مباشر غير موثّق على بنية الجداول بالإنتاج مطلقًا.
- في حالة مشكلة حرجة (Incident): تفعيل صفحة "الموقع تحت الصيانة" مؤقتًا (صفحة ثابتة بسيطة ودّية للطفل، وليس رسالة خطأ تقنية مخيفة) لحين الإصلاح.

---

## 6. المراقبة بعد الإطلاق (Post-Launch Monitoring)

| الأداة | الغرض |
|---|---|
| Google Analytics | أنماط استخدام عامة (بدون تعريف شخصي للطفل) |
| Supabase Dashboard | مراقبة استهلاك قاعدة البيانات والتخزين مقابل الحدود المجانية |
| GitHub Actions Logs | سجل كل عملية نشر ونتائجها |
| تنبيهات يدوية للمدير | من لوحة تحكم المدير نفسها (الأنماط السلوكية المتكررة، المخرج 16 قسم 8) |

---

## 7. خطة الدعم المستمر (بعد الإطلاق)

- مراجعة أسبوعية لسجلات الأخطاء (Error Logs) خلال أول شهر من الإطلاق تحديدًا (أعلى احتمال لظهور مشاكل غير متوقعة).
- قناة تواصل واضحة (بريد/نموذج) لأولياء الأمور للإبلاغ عن أي مشكلة تقنية تخص طفلهم تحديدًا، تصل للمدير مباشرة.

---

## الخطوة التالية والأخيرة

**الخطوة 20 — Roadmap**: خارطة طريق زمنية لتنفيذ كل المخرجات الـ19 السابقة فعليًا (بالكود)، مقسّمة لمراحل قابلة للتنفيذ من الموبايل خطوة بخطوة.

هذا المستند يحتاج مراجعتك واعتمادك قبل بدء الخطوة 20 — وهي **آخر خطوة تخطيط قبل بدء البرمجة الفعلية** حسب قواعد المشروع.
